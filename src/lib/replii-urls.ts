/** Canonical live site + billing API (Next.js on Vercel). */
export const REPLII_PRODUCTION_ORIGIN = "https://replii-lac.vercel.app";

/** Marketing domain — point DNS A record to 76.76.21.21 when ready. */
export const REPLII_MARKETING_ORIGIN = "https://replii.ai";
